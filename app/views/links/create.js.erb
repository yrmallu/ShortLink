var element = document.getElementById('short-link');
element.innerHTML = "<%= @link.shortened_url %>";

var userLinks = document.getElementById('user-links');
var html = "<%= j render 'links/links', links: @user_links %>";
userLinks.innerHTML = html;